import type { Metadata } from 'next'
import { GeistSans } from 'geist/font/sans'
import { GeistMono } from 'geist/font/mono'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

export const metadata: Metadata = {
  title: 'Full Stack ML Engineer | Production ML Systems & MLOps',
  description: 'Experienced ML Engineer specializing in end-to-end machine learning lifecycle, MLOps best practices, and productionizing AI systems. Expert in Python, TensorFlow, PyTorch, and cloud platforms.',
  generator: 'v0.app',
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`font-sans ${GeistSans.variable} ${GeistMono.variable}`}>
        {children}
        <Analytics />
      </body>
    </html>
  )
}
